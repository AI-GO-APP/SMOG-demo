/**
 * components/pending-list.js
 * 待排案件側欄元件
 *
 * v0.3 起：「待排」已搬到「未排案件」獨立頁面（unscheduled-page.js）
 *         此元件保留為 noop 以維持向後相容
 */

const PendingListComponent = (() => {
  function render() {
    // v0.3: 沒事做，已被 UnscheduledPage 取代
  }
  return { render };
})();
